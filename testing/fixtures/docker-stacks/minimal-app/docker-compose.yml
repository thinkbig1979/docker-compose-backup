# Minimal test stack for basic backup operations
version: '3.8'

services:
  simple-web:
    image: httpd:alpine
    container_name: minimal-web
    ports:
      - "8081:80"
    volumes:
      - simple-data:/usr/local/apache2/htdocs
    restart: unless-stopped

volumes:
  simple-data: