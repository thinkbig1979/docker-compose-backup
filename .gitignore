# Logs directory - contains runtime logs and PID files
logs/

# Lock files - created at runtime
locks/

# Go binaries (build artifacts, not the distributed binaries)
cmd/manage-dirlist/manage-dirlist
cmd/docker-backup/docker-backup

# Active config file (contains sensitive data)
config/config.ini

# Supporting documentation (session notes, specs)
Supporting-Docs/